import { Menu } from "./javascript/menu.js";
import { AnimationsCards } from "./javascript/animations.js";
import "./scss/index.scss";

// основная функция, точка входа в приложение
function main() {
    // вызываем класс меню
    const menu = new Menu();
    const animationsCards = new AnimationsCards();
}

// вызываем основную функцию
main();